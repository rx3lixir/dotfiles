general {
  # Can also be "master"
  layout = dwindle 

  # Borders color
  col.active_border   = $surface_variant
  col.inactive_border = $inverse_on_surface

  no_border_on_floating = false
  border_size = 2

  # Defining gaps between windows
  gaps_in  = 4
  gaps_out = 6
}
